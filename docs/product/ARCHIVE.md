# ARCHIVE

This document records old product logic that conflicts with the Contextual School OS model and has been archived.

*Currently empty. Will be populated as we migrate logic during the Contextual OS implementation.*

## Documents Cleanup (Chantier #5/6)
The following generic document generators were removed from the generic `/dashboard/documents` hub because they belong to specific contextual workflows (Grades, Payments):
- `/dashboard/documents/report-card` -> Moved to `/dashboard/grades/report-card`
- `/dashboard/documents/invoice` -> Moved to `/dashboard/payments/invoice`
- `/dashboard/documents/receipt` -> Moved to `/dashboard/payments/receipt`
- `/dashboard/reports` -> Moved to `/dashboard/admin/reports`
